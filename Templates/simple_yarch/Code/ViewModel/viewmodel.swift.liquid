//
//  Created by {{ developer.name }} on {{ date }}.
//

struct {{ module_info.name }}ViewModel {
	let title: String
  
  init(model: {{ module_info.name }}Model) {
    title = "{{ module_info.name }}Title"
  }
}