//
//  {{ module_info.description }}
//  Created by {{ developer.name }} on {{ date }}.
//

enum {{ module_info.name }} {

	enum BaseLogic {
		struct Response {
			var result: Result<{{ module_info.name }}Model, BaseError>
		}

		struct ViewModel {
			var state: State
		}
	}

	enum State {
		case result({{ module_info.name }}ViewModel)
		case error(message: String)
		case loading
		case stopLoading
	}
}
